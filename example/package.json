{
  "name": "web-wake-word-example",
  "version": "1.0.12",
  "description": "An example app using web-wake-word",
  "main": "example.js",
  "scripts": {
    "build": "export LICENSE_KEY=$(cat licensekey.txt) && npx webpack --config webpack.config.js",
    "start": "http-server . -p 8080 --ssl --cert cert.pem --key key.pem",
    "clean": "rm -rf dist"
  },
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "@babel/core": "^7.14.6",
    "@babel/preset-env": "^7.14.7",
    "babel-loader": "^8.2.2",
    "copy-webpack-plugin": "^9.1.0",
    "webpack": "^5.95.0",
    "webpack-cli": "^4.10.0",
    "webpack-dev-server": "^4.15.2"
  },
  "dependencies": {
    "path-browserify": "^1.0.1"
  }
}
